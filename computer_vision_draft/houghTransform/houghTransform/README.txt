The class DrivabilityDetector contains methods for calculating the drivable area in a grayscale image

In main.cpp, tests can be performed. Add folders with images to project folder(or specify the path) to test code.